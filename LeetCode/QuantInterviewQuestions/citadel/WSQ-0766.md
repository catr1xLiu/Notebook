---
source: "https://www.thewallstreetquants.com/ebook#p=767"
publisher: "The Wall Street Quants"
question_number: 766
company: "Citadel"
interview: "Software Engineer Interview"
downloaded_at: "2026-09-17T00:03:29+00:00"
tags:
  - quant-interview
  - citadel
---

> [!example] Question 766 · Citadel
> ## Software Engineer Interview

Build a linked list in Java.

[Source](https://www.thewallstreetquants.com/ebook#p=767)

> [!success]- Solution (Click to expand)
> ## Singly Linked List in Java
>
> A linked list is a linear data structure made of nodes, where each node stores:
>
> - a value
> - a reference to the next node
>
> In a **singly linked list**, each node points only to the next node.
>
> ---
>
> ## Implementation
>
> ```java
> public class LinkedList {
>     // Node class
>     private static class Node {
>         int data;
>         Node next;
>
>         Node(int data) {
>             this.data = data;
>             this.next = null;
>         }
>     }
>
>     private Node head;
>
>     // Insert at the end
>     public void append(int data) {
>         Node newNode = new Node(data);
>
>         if (head == null) {
>             head = newNode;
>             return;
>         }
>
>         Node current = head;
>         while (current.next != null) {
>             current = current.next;
>         }
>         current.next = newNode;
>     }
>
>     // Insert at the beginning
>     public void prepend(int data) {
>         Node newNode = new Node(data);
>         newNode.next = head;
>         head = newNode;
>     }
>
>     // Delete first occurrence of a value
>     public void delete(int data) {
>         if (head == null) {
>             return;
>         }
>
>         if (head.data == data) {
>             head = head.next;
>             return;
>         }
>
>         Node current = head;
>         while (current.next != null && current.next.data != data) {
>             current = current.next;
>         }
>
>         if (current.next != null) {
>             current.next = current.next.next;
>         }
>     }
>
>     // Search for a value
>     public boolean contains(int data) {
>         Node current = head;
>         while (current != null) {
>             if (current.data == data) {
>                 return true;
>             }
>             current = current.next;
>         }
>         return false;
>     }
>
>     // Print the list
>     public void printList() {
>         Node current = head;
>         while (current != null) {
>             System.out.print(current.data + " -> ");
>             current = current.next;
>         }
>         System.out.println("null");
>     }
>
>     // Main method for testing
>     public static void main(String[] args) {
>         LinkedList list = new LinkedList();
>
>         list.append(10);
>         list.append(20);
>         list.append(30);
>         list.printList();
>
>         list.prepend(5);
>         list.printList();
>
>         System.out.println(list.contains(20)); // true
>         System.out.println(list.contains(40)); // false
>
>         list.delete(20);
>         list.printList();
>     }
> }
> ```
>
> ---
>
> ## How it works
>
> ### $1.$ Node structure
>
> Each node is defined by the `Node` class:
>
> ```java
> private static class Node {
>     int data;
>     Node next;
> }
> ```
>
> Here, `data` stores the value, and `next` stores the reference to the next node.
>
> ### $2.$ Appending a node
>
> The `append` method adds a node at the end:
>
> - If the list is empty, the new node becomes the head.
> - Otherwise, traverse to the last node and link the new node there.
>
> ### $3.$ Prepending a node
>
> The `prepend` method adds a node at the front:
>
> - Point the new node to the current head.
> - Update `head` to the new node.
>
> ### $4.$ Deleting a node
>
> The `delete` method removes the first node with the given value:
>
> - If the head holds the value, move `head` to the next node.
> - Otherwise, find the node before the target and skip over it.
>
> ### $5.$ Searching
>
> The `contains` method traverses the list and returns `true` if a node contains the given value.
>
> ---
>
> ## Example output
>
> ```java
> 10 -> 20 -> 30 -> null
> 5 -> 10 -> 20 -> 30 -> null
> true
> false
> 5 -> 10 -> 30 -> null
> ```
>
> ## Final answer
>
> A basic singly linked list in Java can be built using a `Node` class and a `head` pointer, with methods such as `append`, `prepend`, `delete`, `contains`, and `printList`, as shown in the implementation above.
