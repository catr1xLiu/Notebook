---
source: "https://www.thewallstreetquants.com/ebook#p=1074"
publisher: "The Wall Street Quants"
question_number: 1073
company: "IMC"
interview: "Software Engineer Interview"
downloaded_at: "2026-09-17T00:03:29+00:00"
tags:
  - quant-interview
  - imc
---

> [!example] Question 1073 · IMC
> ## Software Engineer Interview

Implement a tic-tac-toe game.

[Source](https://www.thewallstreetquants.com/ebook#p=1074)

> [!success]- Solution (Click to expand)
> ## Tic-Tac-Toe Implementation
>
> A clean way to implement Tic-Tac-Toe is to represent the board as a $3 \times 3$ grid, alternate turns between two players, check for a winner after each move, and stop when either a player wins or the board is full.
>
> ### Python Implementation
>
> ```python
> class TicTacToe:
>     def __init__(self):
>         self.board = [[" " for _ in range(3)] for _ in range(3)]
>         self.current_player = "X"
>
>     def display_board(self):
>         print("\n")
>         for i, row in enumerate(self.board):
>             print(" | ".join(row))
>             if i < 2:
>                 print("--+---+--")
>         print("\n")
>
>     def make_move(self, row, col):
>         if row < 0 or row > 2 or col < 0 or col > 2:
>             print("Move out of bounds. Try again.")
>             return False
>         if self.board[row][col] != " ":
>             print("Cell already occupied. Try again.")
>             return False
>         self.board[row][col] = self.current_player
>         return True
>
>     def check_winner(self):
>         # Check rows
>         for row in self.board:
>             if row[0] == row[1] == row[2] != " ":
>                 return True
>
>         # Check columns
>         for col in range(3):
>             if self.board[0][col] == self.board[1][col] == self.board[2][col] != " ":
>                 return True
>
>         # Check diagonals
>         if self.board[0][0] == self.board[1][1] == self.board[2][2] != " ":
>             return True
>         if self.board[0][2] == self.board[1][1] == self.board[2][0] != " ":
>             return True
>
>         return False
>
>     def is_draw(self):
>         for row in self.board:
>             if " " in row:
>                 return False
>         return True
>
>     def switch_player(self):
>         self.current_player = "O" if self.current_player == "X" else "X"
>
>     def play(self):
>         print("Welcome to Tic-Tac-Toe!")
>         while True:
>             self.display_board()
>             print(f"Player {self.current_player}'s turn")
>
>             try:
>                 row = int(input("Enter row (0, 1, or 2): "))
>                 col = int(input("Enter column (0, 1, or 2): "))
>             except ValueError:
>                 print("Invalid input. Please enter numbers only.")
>                 continue
>
>             if not self.make_move(row, col):
>                 continue
>
>             if self.check_winner():
>                 self.display_board()
>                 print(f"Player {self.current_player} wins!")
>                 break
>
>             if self.is_draw():
>                 self.display_board()
>                 print("It's a draw!")
>                 break
>
>             self.switch_player()
>
>
> if __name__ == "__main__":
>     game = TicTacToe()
>     game.play()
> ```
>
> ## Explanation
>
> The implementation uses the following components:
>
> - The board is stored as a list of lists.
> - The variable `current_player` tracks whose turn it is.
> - The method `make_move` validates and applies a move.
> - The method `check_winner` checks all rows, columns, and diagonals.
> - The method `is_draw` determines whether the board is full without a winner.
> - The method `switch_player` alternates between `"X"` and `"O"`.
> - The method `play` runs the game loop until the game ends.
>
> ## Final Result
>
> This program provides a complete two-player console-based Tic-Tac-Toe game in Python.
